services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      APP_NAME: Apex AI Trading API
      APP_ENV: development
      DEFAULT_TIMEZONE: UTC
    restart: unless-stopped
