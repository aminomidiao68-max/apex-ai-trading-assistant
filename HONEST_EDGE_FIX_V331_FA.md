# گزارش اصلاح v3.31 — لایه صداقت آماری (Honest Edge)

این نسخه مهم‌ترین عیب سیستم را برطرف می‌کند: تناقض بین «نرخ برد اندازه‌گیری‌شده»
و «نرخ برد نمایشی» که به کاربر فروخته می‌شد.

## ۱. صادقانه: آیا سیستم «سودآور» شد؟

نه با یک تغییر کد — و هیچ‌کس نمی‌تواند سودآوری را تضمین کند. کاری که در این نسخه
انجام شد این است که سیستم دیگر **ضرر را پنهان نمی‌کند** و ورود روی ستاپ‌هایی که
اندازه‌گیری خودِ پروژه آن‌ها را ضررده نشان می‌دهد **مسدود** شده است. امید ریاضی
(expectancy) با حذف ستاپ‌های منفی بالا می‌رود، ولی سودآوری واقعی فقط با
اندازه‌گیری مجدد walk-forward و معامله کاغذی طولانی اثبات می‌شود.

## ۲. تغییرات اعمال‌شده روی بک‌اند

| فایل | تغییر |
|---|---|
| `backend/app/services/honest_edge.py` (جدید) | تک‌منبع حقیقت برای نرخ برد. نرخ برد فقط از اندازه‌گیری walk-forward واقعی (`_EDGE_CALIBRATION`، n≥25، با کارمزد). ستاپ اندازه‌گیری‌نشده → «اندازه‌گیری‌نشده»، نه عدد ساختگی. |
| `backend/app/services/strategy_grounded_helper.py` | تمام نرخ‌های برد تبلیغاتی (۵۵٪ تا ۷۶٪) از خروجی کاربر حذف شد؛ مقدار قدیمی فقط در `win_rate_claimed_unvalidated` برای audit می‌ماند. پرامپت AI با «قانون صداقت آماری» تقویت شد (ممنوعیت ساختن وین‌ریت). |
| `backend/app/services/strict_decision_engine.py` | دروازه سخت جدید `measured_edge_not_negative`: ستاپی که pf اندازه‌گیری‌شده‌اش ≤۱ است هرگز actionable نمی‌شود. تیر نمایشی جدید `NEGATIVE_EDGE_WATCH`. خروجی تصمیم شامل `measured_edge` شفاف. |
| `backend/app/services/strategy_pack_v2.py` | هر detector با pf≤۱ یا avgR≤۰ به `watch_only` و `forming` تنزل می‌یابد (قبلاً فقط بدترین دسته). |
| `backend/app/services/orderflow_service.py` | `confidence` هاردکد ۰.۷۰ حذف شد؛ پایه ۰.۴۵ و برچسب صریح `confidence_is_data_completeness` (این عدد کامل‌بودن داده است، نه اطمینان معاملاتی). |
| `backend/app/services/mt5_connector.py` | `ready` هرگز True نمی‌شود چون اجرای MT5 پیاده‌سازی نشده؛ فقط `credentials_configured` گزارش می‌شود. |
| `backend/tests/test_honest_edge_v331.py` (جدید) | ۵ تست که برگشت اعداد تبلیغاتی را برای همیشه می‌شکند. |

## ۳. وضعیت تست

`python -m pytest backend/tests` → **۴۱۹ تست موفق، ۱ skip**.

## ۴. قدم‌های بعدی برای نزدیک شدن به سودآوری واقعی

1. اجرای مجدد کالیبراسیون walk-forward روی داده تازه و چند صرافی (n بزرگ‌تر).
2. تمرکز روی سه ستاپ با edge مثبت اندازه‌گیری‌شده: `triangle_break`، `eqh_eql_raid`، `inverse_hs`.
3. کالیبره کردن «احتمال» با رگرسیون ایزوتونیک روی نتایج بسته‌شده تا برچسب
   `probability_is_calibrated=False` بتواند True شود.
4. معامله کاغذی حداقل ۲۰۰ معامله قبل از هر سرمایه واقعی.
